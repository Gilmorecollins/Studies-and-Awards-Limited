# Dependencies
node_modules/

# Raw, uncompressed source photos and logo files (many MB each).
# The web-ready, compressed versions actually used by the site live in
# site/assets/ and are tracked normally. Keep your own backup of the
# originals in source-assets/ elsewhere (cloud drive, etc.). They are
# not preserved by git here.
source-assets/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
